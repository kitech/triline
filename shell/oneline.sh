#!/bin/sh

# 查找linux系统中的循环链接
# find . -follow -printf ""
# centos默认coredump文件名格式
# find -name "core.[0-9]*"
# 改过的coredump文件名格式
# find -name "*.P[0-9]*.core"
# find -name "*.png" -exec cp -v "{}" ../images/ \;
